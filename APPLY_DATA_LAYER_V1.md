# Data Layer v1

Adds:

- CSV/XLSX import -> `WaterSample`
- SQLite schema and repositories
- atomic `AnalysisService`
- end-to-end `ImportService`
- integration tests for CSV, XLSX, SQLite and the full 300-row RFP fixture
- smoke script that creates `data/database/aqualog.sqlite3`

No GUI code is included yet.
